public class tri_compte_bancaire {

	
	
	
	List<bankAccount> Lba = new
			
			
			public BankAccounts implements Comparable {
		
		
		int CompareTo (bankAccount) {
			
			if(b.getBankAccount() == this.getBankAccount)
				return 0;
			
			if(b.getBankAccount() > this.getBankAccount)
				return 1;
			
			if(b.getBankAccount() < this.getBankAccount)
				return -1;
			
			
		}
	}
}
